//String
let myName: string = 'James Bond';

// number
let size: number = 12;

//boolean
let isValid: boolean = true;

// list string
let listNames: string[] = ['Sadio Mane', 'Youssou Ndour'];

// function with return
function getName(): string {
    return "Me too";
  }
  // no return
  function noReturn(): void {
    console.log('yo, whatsup?');
  }